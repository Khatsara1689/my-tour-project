// file: app/dashboard/admin/page.js
import AdminDashboard from '../components/AdminDashboard'; // Import component ที่เราสร้างใหม่

export default function AdminDashboardPage() {
  // ให้หน้านี้ทำหน้าที่แค่ Render component หลักเท่านั้น
  return <AdminDashboard />;
}